version: "3"
services:
  nginx:
    image: nginx:alpine
    container_name: nginx_web_server
    ports:
      - "8081:80"
    volumes:
      - ./cards:/usr/share/nginx/html:ro,z
    restart: unless-stopped
